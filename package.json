{
  "name": "soloq-challenge",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @challenge/web dev",
    "dev:server": "pnpm --filter @challenge/server dev",
    "build": "pnpm --filter @challenge/web build",
    "doctor": "pnpm --filter @challenge/server doctor",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
